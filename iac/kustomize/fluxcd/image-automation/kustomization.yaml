apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Per-app ImageRepository / ImagePolicy / ImageUpdateAutomation resources will be
# added here (wise-home-index, pms-docker, …). Controllers are in flux-system/.
resources: []
