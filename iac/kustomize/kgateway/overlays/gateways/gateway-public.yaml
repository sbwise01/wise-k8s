apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-public
  namespace: kgateway-system
  annotations:
    # external-dns v0.20 reads target from Gateway (not HTTPRoute).
    # Keep public CNAMEs → apex (WAN A via route53-ddns).
    # Without this, gateway-httproute would publish A → MetalLB VIP (.217).
    external-dns.alpha.kubernetes.io/target: home.bradandmarsha.com
spec:
  gatewayClassName: kgateway
  infrastructure:
    parametersRef:
      name: gateway-public
      group: gateway.kgateway.dev
      kind: GatewayParameters
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
    - name: https-wildcard
      protocol: HTTPS
      port: 443
      hostname: "*.home.bradandmarsha.com"
      tls:
        mode: Terminate
        certificateRefs:
          - name: gateway-home-tls
            kind: Secret
            group: ""
      allowedRoutes:
        namespaces:
          from: All
    # Apex cannot share a listener with the wildcard (apex is not covered by *.).
    # Still the app cert until a follow-up renames this to https-apex on gateway-home-tls.
    - name: https-home
      protocol: HTTPS
      port: 443
      hostname: home.bradandmarsha.com
      tls:
        mode: Terminate
        certificateRefs:
          - name: certificate-wise-home-index
            namespace: default
            kind: Secret
            group: ""
      allowedRoutes:
        namespaces:
          from: All
