apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kubernetes Gateway API standard channel — pinned for kgateway 2.4.x
# (compatible with Gateway API 1.4–1.6). Source:
# https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.6.1/standard-install.yaml
resources:
  - v1.6.1/standard-install.yaml
