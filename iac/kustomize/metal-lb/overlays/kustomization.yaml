apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - home-pool.yaml
  - home-pool-internal.yaml

patches:
  - target:
      kind: DaemonSet
      name: speaker
      namespace: metallb-system
    path: patches/speaker-ceph-toleration.yaml
