apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

patches:
  - target:
      kind: Deployment
    path: patches/deployment.yaml

configMapGenerator:
  - name: app-config
    files:
      - files/applications.yaml
