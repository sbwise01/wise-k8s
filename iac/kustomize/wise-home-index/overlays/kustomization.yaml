apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - image-automation.yaml

images:
  - name: sbwise/wise-home-index
    newTag: 2.1.0 # {"$imagepolicy": "flux-system:wise-home-index:tag"}

patches:
  - target:
      kind: Deployment
    path: patches/deployment.yaml
