apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ingress-nginx-internal

resources:
  - ../../base/v1.14.1

patches:
  - target:
      kind: Service
      name: ingress-nginx-controller
    path: patches/controller-lb.yaml
