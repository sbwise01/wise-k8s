apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - certificate.yaml
  - deployment.yaml
  - httproute.yaml
  - referencegrant-gateway-tls.yaml
  - rbac.yaml
  - pvc.yaml

configMapGenerator:
  - name: nginx-default-conf
    files:
      - files/default.conf
