# Tomcat OIDC state is pod-local; pin browsers to one replica (nginx cookie affinity).
apiVersion: gateway.kgateway.dev/v1alpha1
kind: BackendConfigPolicy
metadata:
  name: acruet-admin-sticky
  namespace: acruet
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: acruet-admin
  loadBalancer:
    maglev:
      hashPolicies:
        - cookie:
            name: acruet-admin-route
            path: /
            ttl: 24h
            httpOnly: true
            secure: true
          terminal: true
