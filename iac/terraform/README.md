# Terraform management

## To Do list
1. Create stacks for bootstrap and homelab
1. Import Delegation Set into bootstrap (src/implementations/delegationsets)
1. Import TF State bucket into bootstrap
1. Create terraform role in bootstrap
1. Convert homelab stack to use terraform role
1. Implement pre-merge plan and post-merge apply GHA workflow
